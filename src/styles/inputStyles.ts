import { css } from "lit-element";

/** Shared styles for inputs */
export const inputStyles = css`
  input[type=text] {
		color: black;
		height: 100px;
		font-size: 32px;
		padding-left: 0.5rem;
	}

	button {
		padding: 10px;
		font-size: 120%;
		font-weight: bold;
		cursor: pointer;
	}
`;
